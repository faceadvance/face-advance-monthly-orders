# Changelog — Face Advance Monthly Orders (ระบบ MO)

สัญลักษณ์: ➕ เพิ่ม · 🔧 แก้บั๊ก · 🔄 เปลี่ยน · ➖ เอาออก
เวลาทั้งหมดเป็นเวลาไทย · ทุก deploy ต้องบันทึกสรุปที่นี่ (ใหม่สุดอยู่บน)

---

## 2026-09-17

### `__HASH__` · 17:00 — "ชำระแล้ว + ตีกลับ" เข้า EDITH · ปุ่มยืนยันตีกลับ · แก้เปิดรูปไม่ได้ · นำเข้าตีกลับย้อนหลัง 1,785 รายการ

**1) 🔧 `recon_conflict` ครอบ "รับเงินแล้ว" ทุกวิธีชำระ ไม่ใช่แค่ COD** *(DB: `migrations/2026-09-17-recon-paid-conflict.sql`)*
- ปัญหา: `reconcile_order` ตั้ง `recon_conflict` เฉพาะเมื่อมี **รายการรับเงิน COD** คู่กับ **ตีกลับถึง** → ออเดอร์ที่รับเงินทางอื่น (โอนเงิน/ตัดบัตร/ตั้ง ชำระแล้ว มือ) แล้วของตีกลับมา **ตกไปสาขาที่สอง พลิกเป็น `ตีกลับ/ยกเลิก` เงียบๆ ไม่ขึ้น EDITH เลย**
- เจอตอนเตรียมนำเข้าตีกลับย้อนหลัง: **8 ออเดอร์ · 11,030 บาท** จะหายจากยอด "ชำระแล้ว" โดยไม่มีใครเห็น
- แก้: เงื่อนไขขัดแย้ง = `has_ret and (has_cod or payment_status = 'ชำระแล้ว')`
- ข้อความต้องตรงเคสจริงทั้ง 3 ที่ (บทเรียนเก่า: **ห้ามเปลี่ยนความหมายของค่าจาก backend เดี่ยวๆ** แล้วปล่อยให้ UI พูดคำเดิม)
  · timeline note · `app_edith_issues` summary (`ขัดแย้ง: ชำระแล้ว (โอนเงิน) แต่ตีกลับถึง`) · การ์ดใน EDITH
- ข้อมูลเดิมไม่กระทบ: ตรวจแล้ว **0 แถว**ในระบบเข้าเงื่อนไขใหม่ก่อนนำเข้า

**2) ➕ `app_edith_confirm_return` — ปุ่ม "ยืนยันตีกลับ · ยกเลิกการขาย"**
- เคสไม่มีรายการ COD กดปุ่ม "ลบ COD" ไม่ได้ (ไม่มีอะไรให้ลบ · RPC ตอบ `not_found`) → เดิมเหลือทางออกแค่ 2 ทาง
- ปุ่มใหม่ให้ผลเท่ากับที่ระบบทำเอง: `ตีกลับ` + `ยกเลิก` + `return_arrived` · เขียน timeline + audit · **ไม่แตะ `no_deduct`** (ค่านี้ตั้งตอนบันทึกตีกลับเท่านั้น)
- 🔄 **frontend `reconResolver` มี 2 หน้าตา**: มี COD → การ์ด "รายการ COD รับเงิน" + ปุ่มลบ COD (เหมือนเดิม) · ไม่มี COD → การ์ด "รับเงินแล้ว — ไม่ผ่าน COD" (วิธีชำระ + สถานะชำระ + callout) + ปุ่มยืนยันตีกลับ
- ทางออกครบ 3 ทางทั้งสองเคส: **เก็บยอด** (ปรับเป็นถึงแล้ว) · **ยกเลิกการขาย** (ยืนยันตีกลับ / ลบ COD) · **ลงผิด** (ลบตีกลับ)
- เทสเองบนเบราว์เซอร์ทั้งสองเคสก่อนรายงาน (เคส COD ไม่ regress · เคสโอนเงินขึ้นข้อความถูก · ปุ่มเรียงตรงกัน)

**3) 🔧 เปิดรูปใน EDITH ไม่ได้ → ขึ้นไอคอนรูปแตกเฉยๆ ไม่บอกอะไร** *(เจ้านายแจ้ง)*
- หาสาเหตุจริง: `drive.google.com/thumbnail?id=X&sz=wN` **302 → `lh3.googleusercontent.com/d/X=wN`** และ lh3 ตอบ **429 / 503 เป็นระยะ** (throttle) — ไม่ใช่เรื่องขนาดรูปหรือไฟล์ถูกปิดแชร์
  · thumbnail (`w600`) มักติดแคชแล้วจึงยังเห็น แต่ lightbox (`w1600`) เป็นคำขอใหม่ → โดน throttle ก่อน
  · ⚠️ วัดด้วย `curl` ตอนแรกได้ 200 ทุกขนาด เพราะ curl ไม่ส่ง `Referer` — ต้องวัดจากเบราว์เซอร์จริงถึงเห็น
- แก้: `openLightbox` ลอง `w1600` → พังแล้วถอยไป `w600` → ยังพังก็แสดง**แผงบอกเหตุ + ชี้ไป "เปิดต้นฉบับ"** แทนไอคอนรูปแตก
- แก้ thumbnail ในการ์ด EDITH ด้วย: พังแล้วขึ้นลิงก์ "โหลดรูปไม่ได้ — เปิดต้นฉบับ ↗"
- 🔴 บทเรียน: ยิงทดสอบรูปถี่เกินทำให้ Google throttle แล้ว**ผลวัดเพี้ยนไปหลายรอบ** (ครั้งหนึ่งสรุปผิดว่า "เฉพาะ `w600` ใช้ได้") — งานวัดของนอกระบบต้องยิงทีละคำขอ เว้นจังหวะ และดู status code ไม่ใช่ดูแค่รูปขึ้น/ไม่ขึ้น

**4) ➕ นำเข้าบันทึกตีกลับย้อนหลัง ม.ค.–ส.ค. 2026 · 1,785 รายการ** *(`database/import-returns-2026/`)*
- ที่มา `database/รายการตีกลับ.xlsx` · รายงานตรวจ `รายการตีกลับ-ตรวจแล้ว.xlsx` (8 ชีต) · เจ้านายเคาะทุกกลุ่ม
  | กลุ่ม | จำนวน | ทำอะไร |
  |---|---|---|
  | พร้อมนำเข้า | 1,727 | นำเข้าตรงๆ |
  | A. ผลตรวจ CHECK ไม่รับ | 4 | `สินค้าสูญหาย`→`สินค้าไม่ครบ` · `บรรจุภัณฑ์เสียหาย`→`สินค้าเสียหาย` · **เก็บคำเดิมไว้ใน `damage_detail`** |
  | B. ไม่มีรายละเอียดเสียหาย | 8 | `damage_detail = 'ไม่มีบันทึกรายละเอียด (นำเข้าย้อนหลัง)'` (CHECK บังคับ · ไฟล์ต้นฉบับไม่มีคอลัมน์นี้) |
  | C. เลขแทร็คซ้ำ | 2 | แถวที่ 2 แก้ `tracking_out` → `7128051759004966` (ออเดอร์ `HIST2605-04313` ลูกค้าคนเดียวกัน) · UNIQUE `btrim(tracking_out)` ยอมรับ |
  | E. ออเดอร์ไม่ใช่ตีกลับ | 41 | นำเข้าให้เข้า EDITH — **39 ใบขัดแย้งตามลอจิก · 2 ใบพลิกเป็นตีกลับถูกต้องอยู่แล้ว** |
  | F. ไม่มีแทร็คตีกลับ+รูป | 3 | `tracking_return = tracking_out` (DB ยอม · ของจริงมีแบบนี้แล้ว 5 แถว) · `photo_url = null` → EDITH แสดง "— ไม่มีรูป" |
  | ขาดในไฟล์ | 6 | ไม่ทำอะไร (ตามที่เจ้านายสั่ง) |
- 🔧 **ล้างข้อมูลสกปรก**: เลขแทร็คตีกลับ 4 แถวติดเศษ float จาก Excel (`880016639611.0`) → ตัด `.0`
- `recorded_at` = **วันที่ของถึงบริษัท** → รอบค่าคอมย้อนหลัง (ตัดวันที่ 26) คิดถูก: ม.ค. 85 · ก.พ. 238 · มี.ค. 265 · เม.ย. 334 · พ.ค. 295 · มิ.ย. 258 · ก.ค. 137 · ส.ค. 173 · (ไม่หักยอดรวม 47)
- ผลข้างเคียงตามลอจิก: **1,738 ออเดอร์ → `ตีกลับ/ยกเลิก`** (ยอดที่ยกเลิก 2,598,988 บาท · `return_arrived=true`) · **47 ใบขัดแย้งเข้า EDITH** (39 COD + 8 โอนเงิน · ยังคงสถานะ ชำระแล้ว ไว้รอคนตัดสิน)
- ตรวจย้อนกับไฟล์ต้นฉบับใหม่หมด (อ่าน Excel ใหม่ ไม่ใช้ staging) **1,785 แถว × 7 ฟิลด์ = 0 ต่าง** · backup `_bak_ret_hist_20260917` (rollback สถานะออเดอร์ได้)
- บันทึกตีกลับในระบบตอนนี้ **1,915 แถว** (7/1/2026 – 15/9/2026)

**5) 🔧 แก้ข้อมูลรายการเดี่ยว (ตามที่เจ้านายแจ้ง)**
- `HIST2602-00215` (`7028021315042626`) ของกลับมาแล้วแต่ไม่มีบันทึก → เพิ่มบันทึกตีกลับ **วันที่ 20/2/2026** (อ้างจากหมายเหตุในออเดอร์เอง) · **ไม่หักยอด** · รอบค่าคอม **ก.พ. 2026** · เขียน timeline กำกับที่มาเพราะไม่มีรูป/เลขแทร็คตีกลับเป็นหลักฐาน · backup `_bak_ret_one_20260917`
  · ทำผ่านหน้าเว็บไม่ได้: `app_save_returns` **บังคับ `photo_url`** และ **ไม่รับวันที่** (จะได้ `now()` → รอบค่าคอมผิด)
- `GOSH0626530061` + `GOSH06277362E1` → **เก็บเงินปลายทาง → โอนเงิน · ชำระแล้ว** (ยอดขาย ก.ค. ย้ายจากรอชำระ 980 บาท) · ทั้งคู่ไม่มีรายการ COD และไม่มีตีกลับ → ไม่เด้งขึ้น EDITH · backup `_bak_pay_method_20260917`
  · 🔴 `app_save_order_tracking` **ไม่รับ `payment_method`** → หน้าเว็บเปลี่ยนวิธีชำระไม่ได้เลย ต้องแก้ทาง DB ทุกครั้ง (ถ้าเคสนี้บ่อย ควรเพิ่มปุ่มให้ Adm)
- อีก 5 รายการกลุ่ม "ขาดในไฟล์" — **ไม่แตะ** ตามที่เจ้านายสั่ง (ไม่มีวันที่ตีกลับถึงให้อ้าง)

### `6d4622b` · 12:00 — ยกเลิก→ผูก payment · COD "ทำเคลม" · scroll ตารางไม่เด้ง · นำเข้า COD ย้อนหลัง

**1) ➕ `delivery = ยกเลิก` → ผูก `payment = ยกเลิก` ให้** *(DB: `migrations/2026-09-17-cancel-couples-payment.sql` · apply แล้ว ~11:10)*
- ปัญหา: `app_bulk_set_delivery` (กดอัพเดตหลายรายการจากตาราง) ผูกให้อยู่แล้ว แต่ `app_save_order_tracking` (แก้ทีละใบใน sidebar) **ไม่ผูก** → พนักงานเลือก "ยกเลิก" ใน sidebar จะได้ **"ยกเลิก + รอชำระ"** ซึ่งไม่ตรงความจริง (ยกเลิกแล้วไม่มีเงินเข้า) และต่างจาก bulk
- แก้: ถ้า delivery เปลี่ยนเป็น `ยกเลิก` และผู้ใช้ไม่ได้เจตนาเลือก payment อื่น (ค่าที่ส่งมาเป็น รอชำระ/ยกเลิก) → ตั้ง `payment = ยกเลิก`
  · **เคารพเจตนาผู้ใช้**: เลือก ชำระแล้ว/ไม่ใช่งานขาย มาเอง จะไม่ทับ (เคสจ่ายแล้วยกเลิกทีหลัง → ต้องคืนเงิน)
- 🔴 **กับดักที่เจอ**: guard `cod_payment_locked` (COD แก้ payment มือไม่ได้) จะบล็อกการผูกนี้กับออเดอร์ COD — **22 จาก 26 ใบที่ยกเลิกเป็น COD** → ใส่ `not v_cancel_couple` ให้ข้าม guard **เฉพาะการผูกอัตโนมัติ** (สอดคล้องกับ bulk ที่ไม่มี guard นี้)
- 🔧 **frontend**: ชิป "สถานะชำระเงิน" เด้งเป็น ยกเลิก ทันทีที่เลือก delivery=ยกเลิก (เห็นก่อนกดบันทึก) · เปลี่ยนใจกลับ → คืนค่าเดิม · ออเดอร์ COD ที่โชว์เป็น badge ล็อกก็อัปเดตตาม (ห่อ `payLock` ใน div เดียวแล้ว re-render)
- ทดสอบ 5 เคสด้วยออเดอร์จริงแบบ rollback ผ่านทุกเคส: COD+ยกเลิก→ยกเลิก ✅ · COD+ตั้งชำระแล้วเอง→บล็อก ✅ · โอนเงิน+ยกเลิก→ยกเลิก ✅ · โอนเงิน+ตั้งชำระแล้วเอง→เคารพ ✅ · ส่งสำเร็จ→ไม่ผูก ✅
- 🔧 **ข้อมูลย้อนหลัง**: แก้ 26 ออเดอร์ (ม.ค. · `ยกเลิก` แต่ `รอชำระ`) → `ยกเลิก` + เขียน timeline · backup `_bak_cancel_pay_20260917` · ยืนยันไม่ได้ส่งจริง (ไม่มีเลขแทร็ค 26/26 · ไม่มีเงิน COD 0/26)

**2) ➕ COD "ทำเคลม" ไม่เปลี่ยนสถานะจัดส่ง** *(DB: `migrations/2026-09-17-cod-claim-no-delivery-change.sql` · apply แล้ว ~10:05)*
- ปัญหา: `app_import_cod_payments` ตั้ง `delivery_status='ส่งสำเร็จ'` ให้ทุกแถวที่นำเข้าจากไฟล์ โดย**ไม่ดู `received_from`** → เคสเคลมขนส่ง (ของหาย/เสียหาย · ขนส่งจ่ายชดใช้ · **ลูกค้าไม่ได้ของ**) ถูกกลืนเป็น "ส่งสำเร็จ"
- แก้: เพิ่มเงื่อนไขเดียว `and e.received_from is distinct from 'ทำเคลม'` (payment ยังตั้ง ชำระแล้ว เพราะเงินเข้าจริง)
- พบตอนนำเข้า COD ย้อนหลัง — 5 ออเดอร์ "มีปัญหา · รับเงินเคลมแล้ว" · ผลกับข้อมูลเดิม **0 แถว** (`ทำเคลม` ยังไม่เคยมีใครใช้: ระบบ 12,533 · ขนส่ง 1,554 · ทำเคลม 0)
- 💡 หมายเหตุ: `reconcile_order` (COD แก้มือ/trigger) ไม่แตะ `delivery_status` อยู่แล้ว → แก้ที่เดียวพอ สองเส้นทางสอดคล้องกันเอง

**3) 🔧 ตารางออเดอร์ไม่เด้งขึ้นแถวแรกเวลากรอง/ค้นหา**
- เดิม ช่องค้นหา/ตัวกรอง/เรียง เรียก `renderTable()` เปล่าๆ → เข้า `else { wrap.scrollTop = 0 }` ทุกครั้ง **รวมทั้งตอนลบคำค้น/ลบตัวกรอง**
- กติกาใหม่ (ตามที่ boss กำหนด):
  · **ช่องค้นหา** → ล้างคำค้นแล้ว **กลับตำแหน่งก่อนเริ่มค้น** (จำ scrollTop + แถวบนสุด ณ ตอนเริ่มพิมพ์)
  · **ตัวกรอง / เรียง / สลับคอลัม** → **ยึดแถวบนสุดที่เห็น** (แถวนั้นอยู่จุดเดิมบนจอ — แนวเดียวกับ Google Sheets)
  · แถวที่ยึดไว้หลุดจากตัวกรอง → ขึ้นบนสุด (กันไม่ให้ `prevScroll` เกินความสูงชุดใหม่แล้วถูก clamp ไปค้างก้นตาราง)
  · เปลี่ยนคำค้นไปเรื่อยๆ / เปลี่ยนเดือน → ขึ้นบนสุดตามเดิม (ถูกแล้ว เป็นชุดข้อมูลใหม่)
- `renderTable` รับ option เพิ่ม `restoreScroll` / `restoreAnchor` / `topIfAnchorGone` (optional — เส้นทางเดิมไม่กระทบ)
- 🔴 **บทเรียน**: รอบแรกส่งงานโดยไม่ได้เทสเอง แล้วให้ boss ไปลอง — มีบั๊ก `preSearch = null` วางนอก `if` ทำให้ค่าที่จำไว้ถูกล้างทุกครั้งที่ debounce ยิง (คือหลังพิมพ์ตัวแรก 160ms) พอลบคำค้นจริงจึงไม่มีอะไรให้คืน · **เทสเองครั้งเดียวก็เจอ** → ต่อจากนี้เทสก่อนรายงานทุกครั้ง
- เทสจริงบน dev server: ค้นหา 89,263→0→**89,263** ✅ (คืนใน 239ms ไม่มีกระพริบ) · กรอง Thailand Post เหลือ 10 แถว → เอาตัวกรองออก **ยึดแถวบนสุดที่กรองได้** ✅ · เรียง A→Z ✅

**4) ➕ นำเข้า COD ย้อนหลัง ม.ค.–มิ.ย. 2569** *(DB เท่านั้น · ไฟล์ `database/COD 2026 1-6.xlsx`)*
- **43,238 แถว · 72,549,178 บาท** (ไฟล์ 43,230 + แยกแถวปนแบรนด์ 8)
- **แยกแถว COD ให้ตรงกับออเดอร์ที่แยกเมื่อวาน** — ขนส่งเก็บเงินครั้งเดียวเต็มจำนวนต่อพัสดุ แต่ออเดอร์ถูกแยก 2 ใบ → `<trk>` = ยอดใบ A · `<trk>-B` = ยอดใบ B (ยอด A+B = ยอดที่รับ ตรงทั้ง 8 คู่ · ยอดรวมไม่เปลี่ยน)
- ปัญหาที่บล็อก **0 ทุกข้อ** · ยอดไม่ตรง **0** · `recon_conflict` **0**
- เคลม 5 ใบคงสถานะ "มีปัญหา" ครบ (ผลจากข้อ 2) · 26 ใบสถานะ "มีปัญหา" → ส่งสำเร็จ (เงิน COD เข้าครบตรงยอด)
- ตรวจย้อนกับไฟล์ต้นฉบับ: แทร็คขาด 0 · เกิน 0 · ยอดไม่ตรง 0 ✅ · backup `_bak_cod_hist_import` 86,507 แถว

**5) ➕ ข้อมูลจำลอง ธ.ค. 2025 สำหรับเทส** *(DB เท่านั้น)*
- 10,000 ออเดอร์ · 14,748 รายการสินค้า · 7,630 ลูกค้า · 16,973,639 บาท · สัดส่วนเลียนแบบของจริง
- `order_no` = `MOCK2512-xxxxx` · แทร็ค `MOCKTRK…` · เบอร์ `0999000001–0999007000` · **ชนกับข้อมูลจริง 0** · ⚠️ **ต้องลบเมื่อเทสเสร็จ** (`_bak_mock_dec2025` เก็บ id ครบ)

---

## 2026-09-16

### `4648c41` · 17:01 — EDITH › Dedup: ปุ่มรวมทันที + แยก "ตรงกัน/คล้ายกัน" + แก้ข้อมูลติดกัน · ⓘ การ์ด KPI

**1) ➕ ปุ่ม "รวมทันที" (หน้า EDITH › Dedup)**
- ปุ่มเดียวจบ **ไม่ถามยืนยัน** (boss สั่ง) — บอก id ที่จะเก็บ **บนตัวปุ่ม** พร้อมเหตุผล+วันที่ เช่น `รวมทันที — เก็บ id #42855 (ลูกค้าใหม่ก่อน 8/5 00:00)` เพราะไม่มี popup ให้ทาน ต้องอ่านได้ก่อนกด (การรวมลบ record ถาวร)
- กฎเลือกฝั่งที่เก็บ: **"ลูกค้าใหม่ก่อน"** = ออเดอร์แรกเกิดก่อน (ลูกค้าเก่ากว่าอยู่รอด)
- fallback ครบ: วันเท่ากัน → id เลขน้อยกว่า (record เก่ากว่า) · ฝั่งเดียวมีออเดอร์ → เก็บฝั่งนั้น · วันอ่านไม่ได้ทั้งคู่ → id น้อยกว่า
- วัดกับคิวจริง 207 คู่: ตัดสินด้วยวันได้ครบ 207 · ผลกระจาย 111 เก็บ id น้อย / 96 เก็บ id มาก (ยืนยันว่าตัดสินด้วย**วัน** ไม่ใช่เลข id)
- **ทีละรายการ** ไม่มีปุ่มรวมทั้งหมด (boss กำหนด)

**2) 🔄 ย้ายปุ่ม "ไม่ใช่คนเดียวกัน (ไม่รวม)" มาแถวเดียวกับ "รวมทันที"**
- 2 ปุ่มตัดสินเร็วอยู่บรรทัดเดียว (เดิมปุ่มไม่รวมอยู่ล่างสุดต้องเลื่อนลง) · คำอธิบายขึ้นบรรทัดใหม่เสมอ
- ปุ่มไม่รวม**ยังถามยืนยันเหมือนเดิม** (เปลี่ยนแค่ตำแหน่ง) · จอ <560px ปุ่มขยายเต็มความกว้าง
- ➖ ลบ CSS กำพร้า `.ed-dismiss-row`

**3) 🔧 บั๊กค่าหลายอันแสดงติดกันจนอ่านไม่ออก**
- สาเหตุ: `.ed-kv .ed-v{display:block}` (2 คลาส) **specificity สูงกว่า** `.ed-vlist{display:flex;flex-direction:column}` (1 คลาส) → ทับ flex column ทิ้ง ค่าจึงไหลต่อกันเป็นบรรทัดเดียวไม่มีตัวคั่น
- อาการจริง: ชื่อกลายเป็น `คุณประเสริฐ C007คุณประเสริฐ j06` · ที่อยู่ 2 ชุดชนกันเป็น `…กรุงเทพมหานคร 1015035 / 88 ม.1…`
- แก้: `.ed-kv .ed-vlist{display:flex}` (เพิ่ม specificity) + **จุดนำเฉพาะเมื่อมี >1 ค่า** (`:not(:only-child)::before`) + line-height 1.35→1.45 · จุดนำของค่าที่ไฮไลต์อยู่**นอก**ป้ายเหลือง เรียงตรงกับจุดอื่น

**4) ➕ แยก "🎯 ตรงกัน" ออกจาก "≈ คล้ายกัน <คะแนน>"**
- ปัญหา: ตัวจับ (`detect_customer_duplicates`) ใช้ **ความคล้าย** (trigram) แต่สีในการ์ดใช้ **ตรงเป๊ะ** → **35/205 เคสในคิวไม่มีสีเลย** ทั้งที่เข้าเงื่อนไขถูกต้อง พนักงานไม่รู้ว่าระบบจับที่จุดไหน
- แก้: frontend อ่านฟิลด์ใหม่ `sim` จาก RPC แล้วแยกแสดง
  - `hit:true` → **🎯 ตรงกัน** ป้ายทึบเหลือง (ยืนยันได้)
  - `hit:false, sim:0.60` → **≈ คล้ายกัน 0.60** เส้นประจาง + ป้ายคะแนนต่อท้ายค่า (ยังไม่ยืนยัน)
- ผลวัดจริง: ไม่มีสีเลย **35 → 0** · สีตรงกับ `reason` ที่ทำให้เข้าคิว **81% → 100%** · ไม่มีรายการที่สีหาย (0 regression)
- 🔴 **บทเรียน (พลาด 1 รอบ แล้ว rollback):** รอบแรกลองยัด fuzzy ลงในธง `hit` เลย เพราะคิดว่า "ไม่กระทบ frontend" — **ผิด** เพราะข้อความ `🎯 ตรงกัน` อยู่ใน `edith.ts` แก้จาก backend ไม่ได้ → ป้ายกลายเป็น**ยืนยันสิ่งที่ไม่จริง** (`เอ๋` ⟷ `เอ็ม` คล้าย 0.60 แต่ป้ายบอก "ตรงกัน") ซึ่งอันตรายกว่าไม่มีสี เพราะพนักงานอาจกดรวมตามป้าย · boss จับได้ → rollback ทันที แล้วทำใหม่เป็น **ฟิลด์แยก `sim`** ไม่แตะความหมายของ `hit`
  → **กฎที่ได้: ถ้า frontend เป็นคนตัดสิน "ข้อความ/การตีความ" ห้ามเปลี่ยนความหมายของค่าจากฝั่ง backend เดี่ยวๆ**

**5) ➕ ⓘ การ์ด KPI** (ค้างจาก 2026-09-15)
- ไอคอน ⓘ มุมขวาล่างของการ์ด KPI · ชี้แล้วขึ้นทันที (ใช้กลไก `.kpitip` ของแอป ไม่ใช่ `title` ที่ต้องรอ ~1 วิ) บอกว่าตัดรายการ **"ไม่ใช่งานขาย"** ออกกี่รายการ — ตอบคำถาม boss ว่าทำไมเลขบนการ์ดไม่ตรงกับตาราง

**6) ➕ DB migration (apply แล้วเมื่อ 16:18 · ไม่ผูกกับ deploy นี้)**
- `database/migrations/2026-09-16-dedup-hit-fuzzy.sql` — เพิ่มฟิลด์ `sim` ใน `_dedup_vals` + helper `_dedup_key` (เทียบแบบไม่สนช่องว่าง/วรรคตอน `. , / ( ) _ '' " | - – —`)
- `sim` ใส่มาเฉพาะเมื่อ "ไม่ตรงเป๊ะ แต่คล้ายถึงเกณฑ์ที่ตัวจับใช้": ชื่อ ≥0.60 · ที่อยู่ ≥0.85 · เบอร์ไม่มี fuzzy
- 🔒 **backward compatible** — `hit` ความหมายเดิม + frontend เก่าข้าม `sim` → deploy ลำดับไหนก็ไม่พัง
- ผลข้างเคียงที่ตรวจแล้ว: `hit` เปลี่ยน false→true **8 เคส** เพราะตัดวรรคตอน (เช่น `67 หมู่.13` vs `67 หมู่13`) — ตรวจทีละเคสยืนยันว่าเป็นที่อยู่เดียวกันจริง ป้าย "ตรงกัน" พูดความจริง
- มีสคริปต์ rollback + บันทึกบทเรียนในคอมเมนต์หัวไฟล์

---

### DB migration (ไม่มี deploy frontend) · 15:05 — นำเข้าข้อมูลย้อนหลัง ม.ค.–มิ.ย. 2569 ทั้งหมด
- ➕ **นำเข้า 48,608 ออเดอร์ · 74,286 รายการสินค้า · ยอดขาย 80,610,554 บาท** จาก `database/2026.xlsx` (ม.ค. 6,880 · ก.พ. 8,573 · มี.ค. 9,103 · เม.ย. 8,786 · พ.ค. 8,371 · มิ.ย. 6,895) · `order_no` = `HIST26MM-NNNNN` (รูปแบบต่างจาก `OD…` ของ GoSell เพื่อให้รู้ว่าเป็นข้อมูลย้อนหลัง)
- ➕ **ลูกค้าใหม่ 29,362 ราย** (แยกตาม brand+phone) · รันตรวจลูกค้าซ้ำครั้งเดียวหลังนำเข้าครบ → **เพิ่มคิวใน EDITH 291 คู่** (รวมเป็น 296) รอพนักงานตรวจ
- ➕ **สินค้าใหม่ 4 รายการ** (ของแถม): `ถ้วยชาสีเหลือง` · `แถมฟรี แก้วกาแฟเซรามิค` · `ผ้าคุมผมนาโน` → แบรนด์อื่นๆ · `Hopeful ร่ม` → HOPEFUL
- 🔄 **แปลงค่าให้เข้ากฎของ DB**
  - `payment_method`: `COD`→เก็บเงินปลายทาง (44,767) · `TRANSFER`→โอนเงิน (3,833)
  - `delivery_status`: ได้รับสินค้าแล้ว→**ส่งสำเร็จ** 47,034 · พัสดุตีกลับ→**ตีกลับ** 1,515 · ยกเลิกออเดอร์→**ยกเลิก** 26 · ตีความไม่ได้ 5 แบบ→**มีปัญหา** 33 (เก็บข้อความเดิมลง `status_detail`)
  - `payment_status`: ส่งสำเร็จ→ชำระแล้ว 46,903 · ตีกลับ/ยกเลิก/มีปัญหา→รอชำระ 1,569 · ตัวแทน→ไม่ใช่งานขาย 136 · **ไม่ตั้ง `ยกเลิก` เองตามกฎ** (ยกเลิกเกิดตอนบันทึกตีกลับเท่านั้น)
  - `return_reason` **1,460 รายการ** map ตรงกับ `RETURN_REASONS` ในระบบทั้ง 9 แบบ (รายละเอียดเพิ่มจากคอลัม "อื่นๆ ระบุ" ลง `status_detail` 718 รายการ)
  - ที่อยู่ 1 ช่อง → แยก `addr_detail`/ตำบล/อำเภอ/จังหวัด/ไปรษณีย์ **สำเร็จ 48,600/48,600 = 100%**
- ➕ **ตัวแทน 136 ออเดอร์** — 68 มีสินค้า `เอกสาร` ในไฟล์อยู่แล้ว + 68 คัดเพิ่มโดยเทียบเบอร์/ชื่อ/ที่อยู่กับตัวแทน 24 รายในระบบ (56 ตรงเบอร์ + 12 เจ้านายยืนยัน) → เติม `เอกสาร` ให้ · ทุกใบ `seller_id` ว่าง + `ไม่ใช่งานขาย` (ลายเซ็นเดียวกับตัวแทน 57 ใบเดิม)
- ➕ **ออเดอร์ปนแบรนด์ 8 ใบ → แยกเป็น 16 ใบ** — ใบ HOPEFUL เก็บเลขพัสดุจริง · ใบแบรนด์อื่นๆ ใช้ `<เลขจริง>-B` (ยอดแยกตามที่เจ้านายกำหนด รวมแล้วเท่าเดิมทุกคู่)
- 🔧 **แก้ข้อมูลผิดพลาดจากไฟล์ต้นทาง 4 จุด** (ตามที่เจ้านายเคาะ) — ม.ค. แถว 2106 ช่องเบอร์เป็นวันที่ → `0875402122` (กู้จาก มิ.ย. แถว 1715 ที่อยู่ตรงกันเป๊ะ) · มี.ค. แถว 8898 ช่องวันที่เป็น `6.0` → `31/3/2026` · เลขพัสดุซ้ำ 1 คู่ → ใช้เลขจริงที่เจ้านายให้มา 2 เลข
- 🔄 **ช่องเลขพัสดุที่เป็นข้อความ 35 แถว** (ยกเลิกออเดอร์ 26 · งานส่งแมส 7 · มารับเองที่ออฟฟิศ 2) → `tracking_no` ว่าง + เก็บข้อความเดิมลง `note`
- 🔧 **`payment_status` ออเดอร์ตัวแทน ก.ค.–ส.ค. 2 ใบ** ที่ยังเป็น "ชำระแล้ว" → **ไม่ใช่งานขาย** (`OD260708169431`, `OD260815176637`) · backup `_bak_agent_paystatus_20260916` · **เดือน ก.ย. 7 ใบไม่แตะ** (พนักงานจัดการเอง)

**ผลกระทบต่อระบบ (วัดจริง)**
- ขนาด DB **81 MB → 133 MB** · `orders` 35→57 MB · `order_items` 8→16 MB
- เวลาโหลดตารางออเดอร์ **ไม่ช้าลง กลับเร็วขึ้น** (หลัง `ANALYZE`) — ส.ค. 6,767 แถว **646 → 466 ms** · เดือนใหญ่สุด มี.ค. 9,103 แถว **601 ms** · ก.ย. **238 ms** · ยังเป็น index scan บน `orders_ordered_at_idx` ทั้งหมด (โหลดทีละเดือน → ขนาดตารางรวมไม่มีผล)
- นำเข้ารายวันปกติยังเร็ว (ลูกค้าใหม่ ~100–200 ราย → ตรวจซ้ำ ~1.2 s ห่างจาก timeout 20 s มาก)

**วิธีนำเข้า / rollback**
- นำเข้าฝั่ง DB ตรงด้วย `psql` (role `postgres`, ไม่มี timeout) ก้อนละเดือน ผ่าน staging `_stg_hist` + gate ตรวจ 8 ข้อก่อนเขียนทุกก้อน — **ไม่ใช้ `app_import_orders`** เพราะ RPC วน insert ทีละแถว (0.932 ms/ออเดอร์ = 45 s) + `detect_customer_duplicates` (17.5 นาที) เกิน `anon` timeout 20 s หลายสิบเท่า
- rollback: `_bak_hist_import` เก็บ id ของออเดอร์ 48,608 + ลูกค้า 29,362 ที่สร้างใหม่ทั้งหมด · ลบได้ด้วย `order_no like 'HIST26%'`

---

## 2026-09-15

### `c6eec24` · 16:40 — แก้โน้ตตัวเองไม่ได้ + ชื่อที่แสดงค้างค่าเก่า + UX ปุ่มบันทึก
- 🔧 **แก้ไขโน้ตของตัวเองไม่ได้** (เคสอ้อม) — เดิมเช็คสิทธิ์ด้วยการ**เทียบชื่อที่แสดง** ที่ cache ไว้ตอนล็อกอิน กับชื่อผู้เขียนโน้ต · admin เปลี่ยน "ชื่อที่แสดง" หลังผู้ใช้ล็อกอินแล้ว → ชื่อในเครื่องค้างค่าเก่า → เทียบไม่ตรง → ปุ่มดินสอไม่โผล่
  → เลิกเทียบชื่อ · **server ส่งธง `mine`** มาให้ (เทียบ `created_by` = user id ของ session) ใน `app_get_order_tracking` + `app_save_order_tracking` · เปลี่ยนชื่อผู้ใช้ทีหลังก็ไม่พังอีก
- 🔧 **ชื่อมุมขวาบนยังเป็นชื่อเก่า** ทั้งที่ admin เปลี่ยนแล้ว — `get_months` คืน `display_name` ปัจจุบัน → หน้าเว็บอัปเดตชื่อในเครื่อง+หัวเว็บทุกครั้งที่เข้าแอป (ไม่ต้องล็อกอินใหม่)
- 🔧 **ประวัติ/โน้ตเก่าแสดงชื่อเดิม** — timeline เปลี่ยนมาแสดง **ชื่อปัจจุบัน** (join `app_users` ด้วย `created_by`) · ถ้าหา user ไม่เจอ (รายการของระบบ `created_by` ว่าง) ใช้ชื่อ snapshot เดิม · **ไม่แก้ข้อมูลเดิมเลย** — เปลี่ยนแค่ตอนอ่าน (เปลี่ยนชื่อในอนาคตก็ตามทันที) · มีผลกับรายการเก่า 742 (Aom-pinchaya→อ้อม) + 5 (Friday→ระบบ)
- 🔧 **กดบันทึกแล้วเหมือนไม่ทำงาน · ต้องกดหลายรอบ · หน่วง** — ปิดปุ่ม + ขึ้น "กำลังบันทึก…" **ทันทีที่กด** (ก่อนรอ network) กันกดซ้ำและเห็นว่าระบบรับคำสั่งแล้ว · ปุ่มคืนสภาพครบทุกเส้นทาง (validate ไม่ผ่าน/error/เน็ตหลุด — เดิมค้างได้)
- 🔧 ตรวจเวอร์ชั่นก่อนบันทึกใส่ **timeout 900ms** — เน็ตช้า/ไม่ตอบ = บันทึกไปเลย ไม่หน่วงผู้ใช้ (ตัวเฝ้าทุก 5 นาที/โฟกัสแท็บ ยังจับเวอร์ชั่นใหม่ให้)
- 🔄 popup ตอนถูกบล็อกเปลี่ยนข้อความให้ชัด: **"ยังไม่ได้บันทึก — ระบบมีการอัปเดต"** + บอกว่าข้อมูลถูกเก็บไว้แล้ว กดตกลงเพื่อรีเฟรชแล้วกดบันทึกอีกครั้ง · popup ที่หลุดหายจะโชว์ใหม่ได้ (เดิมถ้าหลุด = บันทึกถูกบล็อกเงียบๆ)
  - _สาเหตุที่อ้อมเจอ: ล็อกอิน 13:24 · deploy 14:57 → ตอนบันทึก 15:30+ ระบบตรวจเวอร์ชั่นบล็อกไว้ถูกต้อง แต่ข้อความสื่อไม่ชัด_

### `7bdb2e8` · 14:57 — คัดลอกข้อมูลหน้าตีกลับ + หมวดหมู่แจ้งเตือนตามสิทธิ์
**1) ➕ คัดลอกข้อมูล (หน้าออเดอร์ตีกลับ)**
- ปุ่ม **"คัดลอกข้อมูล"** ถัดจากช่องค้นหา — คัดลอกทุกแถวที่แสดงอยู่ (ตามตัวกรอง/คำค้น/การเรียง) เป็นแพทเทิร์น `เบอร์โทร : / ชื่อลูกค้า : / ยอดขาย : / เลขแทร็ค :` คั่นแต่ละรายการด้วย `---`
- ➕ **คอลัม "เลือก" ขวาสุด ตรึงไว้** (เหมือนคอลัมอัพเดตหน้าออเดอร์) — ติ๊กทีละแถว หรือ **คลิกค้างแล้วลาก**เลือกหลายแถว · ลากซ้ำบนที่เลือกไว้ = ยกเลิกเป็นชุด
- เลือกไว้ → ปุ่มเปลี่ยนเป็น **"คัดลอก (n)"** คัดลอกเฉพาะที่เลือก · ไม่ได้เลือก → คัดลอกทั้งหมดที่แสดงอยู่
- หัวคอลัมมี **ป้ายจำนวน + ปุ่ม ✕ ล้างการเลือก** · ข้อมูลในตารางเปลี่ยน (กรอง/ค้นหา/เรียง/เดือน/โหมด) → **ล้างการเลือกอัตโนมัติ**

**2) ➕ หมวดหมู่แจ้งเตือน + กรองตามสิทธิ์เข้าถึงหน้า**
- แบ่งเป็น **3 หมวด**: 📦 ตีกลับ · 📥 นำเข้าออเดอร์ · 💰 นำเข้าไฟล์ COD (ใหม่) · แต่ละหมวดมีไอคอน+สีของตัวเอง
- ➕ **ชิปกรองหมวด** ในกล่องแจ้งเตือน (โชว์เมื่อมี ≥2 หมวด · มีจำนวนต่อหมวด)
- 🔒 **กรองตามสิทธิ์ที่ server** (`app_can_page` + `app_notifications`) — ตีกลับ: ต้องเข้าถึงหน้าออเดอร์ตีกลับหรือออเดอร์ · นำเข้าออเดอร์/COD: ต้องเข้าถึงหน้าออเดอร์ → **RT+ / RTs ไม่เห็นเรื่องนำเข้าอีก**
- COD: เฉพาะที่นำเข้าจากไฟล์ (ข้ามการรับเงินแก้มือทีละรายการ) · limit 30 → **50** (มี 3 หมวดแล้ว)
- ⚠️ **บันทึกเหตุการณ์:** ระหว่างพัฒนา ฟรายเดย์อัปเดต RPC บน prod ก่อน deploy frontend → โค้ดเก่าไม่รู้จัก `kind='cod'` ทำให้แจ้งเตือน COD แสดงผิดเป็น "บันทึกตีกลับเพิ่ม" ให้ผู้ใช้ทุกคนเห็นชั่วคราว · ถอนออกทันทีแล้วใส่กลับพร้อม deploy รอบนี้ · **บทเรียน: การเปลี่ยนที่ frontend ต้องอ่านค่า ให้ deploy frontend ก่อนเสมอ**

## 2026-09-14

### `90d12fa` · 12:11 — ตารางไม่เด้งเวลาบันทึก + ระบบตรวจเวอร์ชั่น + คัดลอกบรรทัดเดียว
**1) 🔧 ตารางอยู่ตำแหน่งเดิมเวลาบันทึก/อัปเดต (ไม่เด้งกลับแถวแรก)**
- `renderTable({ keepScroll, anchorId })` — จำตำแหน่ง scroll + **ยึดแถวที่เพิ่งแก้ให้อยู่จุดเดิมในจอ** แม้แถวข้างบนจะหาย/เพิ่ม · แถวที่กางไว้**ไม่ยุบ**แล้ว
- เดิมเด้งขึ้นบนสุดเมื่อ: บันทึกตอนมี**ตัวกรอง/เรียงบนคอลัมสถานะ** · แก้หลายรายการ (bulk)
- กรอง/เรียง/ค้นหา/เปลี่ยนเดือน → ยังขึ้นบนสุดเหมือนเดิม (ถูกต้องแล้ว)
- _บั๊กที่เจอระหว่างแก้: ต้องตั้ง `scrollTop` **หลัง** spacer มีความสูงจริง ไม่งั้นเบราว์เซอร์บีบเหลือ 0 (ตอนนั้น tbody ยังว่าง)_
- เทสจริงเดือน พ.ค. 12,000 แถว เลื่อนกลางตาราง (50% · 291,420px): บันทึกแล้วขยับ **0px** · แถวอยู่จุดเดิม 670→670px

**2) 🔄 แก้สถานะหลายรายการ (bulk) เร็วขึ้น + ไม่เด้ง**
- เลิก `loadMonth` โหลดใหม่ทั้งเดือน → **อัปเดตเฉพาะ id ที่แก้**ในหน้า + คำนวณ KPI ใหม่ (ตรงกับที่ RPC ทำ · ตรวจเทียบ DB แล้ว)

**3) ➕ ระบบตรวจเวอร์ชั่น — กันพนักงานใช้โค้ดเก่าค้างหลัง deploy (ใหม่)**
- เวอร์ชั่น = **git commit hash ตอน build** → ฝังในบันเดิล + เขียน `version.json` (~20 bytes) · ไม่ต้องแตะ DB · ตรงกับ hash ใน CHANGELOG
- เช็คเมื่อ: **ทุก 5 นาที** (ข้ามตอนแท็บอยู่เบื้องหลัง) · **กลับมาโฟกัสแท็บ** · **ก่อนบันทึกทุกจุด** (throttle 30 วิ) · **ตอน session หลุด/idle** · **หลังล็อกอิน**
- เจอเวอร์ชั่นใหม่ตอนกำลังใช้งาน → **บล็อกการบันทึก** + popup ปิดไม่ได้ (ไม่มี × · คลิกนอก/Esc ไม่ได้) ปุ่ม "ตกลง" → รีเฟรช
- หลังรีเฟรช → **กลับเดือนเดิม + เปิด sidebar ออเดอร์เดิม + กู้ค่าที่กรอกไว้ครบ** → กดบันทึกต่อได้เลย ไม่ต้องพิมพ์ใหม่
- session หลุด/idle timeout หรือล็อกอินด้วยโค้ดเก่า → **รีเฟรชเงียบๆ** (แก้ช่องเดิมที่เด้งหน้า login แต่โค้ดยังเก่า)
- บล็อกครบ **6 จุด**: sidebar อัปเดตออเดอร์ · ดินสอ inline · bulk · บันทึกตีกลับ · นำเข้าออเดอร์ · นำเข้า COD (+แก้มือ) — ทุกจุดเซฟร่างก่อนเด้ง
- ไฟล์ใหม่ `frontend/src/version.ts` + `vite.config.ts` (plugin `fa-version-json` · dev server เสิร์ฟให้เทสได้)

**4) ➕ คัดลอกในการ์ด "ข้อมูลออเดอร์" (sidebar) ได้บรรทัดเดียว**
- ลากเลือกชื่อ+เบอร์ → คัดลอกได้ `คุณ... j01 0814936916` (เดิมมี `\n` คั่น เอาไปวางในแชตขึ้น 2 บรรทัด)
- ทำด้วยการ**ดัก event `copy`** แปลงขึ้นบรรทัดใหม่เป็นเว้นวรรค — **ไม่แตะ HTML/CSS ของ UI เลย** (หน้าจอยังคนละบรรทัดเหมือนเดิม)
- เฉพาะการ์ดข้อมูลออเดอร์ · การ์ดอื่น (โน้ต/ประวัติ/สถานะ) คัดลอกตามปกติ

**5) 🔄 ประวัตินำเข้า** — หัวคอลัม "ตั้งแต่/ถึง" จัดตรงแนวกับข้อมูล (ชิดขวา/ชิดซ้าย)

### `2c00478` · 10:48 — ประวัตินำเข้า: ใส่ขีดคั่นช่วงวันที่
- 🔄 ตารางประวัตินำเข้า (ออเดอร์) ใส่ **ขีด `–` คั่นหน้าวันที่ปลายช่วง** → อ่านเป็น **`1/7/2026 – 31/7/2026`** เห็นชัดว่าเป็นช่วงข้อมูล · ขีดสีเทาอ่อนไม่แย่งสายตา · ตารางยังพอดีช่องไม่ล้น

### `805d1a8` · 10:40 — ย้ายปุ่ม "เลือกประเภทอื่น" ขึ้นหัว modal
- 🔄 ปุ่ม **"‹ เลือกประเภทอื่น"** เดิมเป็นแถบข้อความกินพื้นที่ทั้งแถวในเนื้อหา → ย้ายเป็น **ปุ่มลูกศรกลม ‹ ในหัว modal** (ซ้ายชื่อเรื่อง · hover เป็นน้ำเงินเลื่อนซ้ายเล็กน้อย) · เนื้อหาไม่ถูกดันลง modal เตี้ยลง
- ℹ️ โชว์เฉพาะหน้าย่อย (เลือกไฟล์/COD/error) · **ซ่อนตอนตรวจไฟล์/preview** กันกดแล้วผลตรวจหาย
- 🔧 หน้า error (ไฟล์ผิดนามสกุล) เดิมปุ่มย้อนกลับกดไม่ได้ในบางทาง — แก้แล้ว
- ℹ️ ประวัตินำเข้าเยอะขึ้น: เลื่อนในกรอบ (สูงคงที่ 418px) · หัวตารางติดหนึบ · modal ไม่ยืดเกินจอ · ดึง **50 ล่าสุด** (เจ้านายเคาะว่าพอ ไม่ต้องเพิ่ม)

### `6ef5c69` · 10:32 — ตารางจัดระดับข้อความ + modal นำเข้าเป็นหน้าเดียวแบ่งซ้าย/ขวา
**1) ตารางออเดอร์: ข้อความทุกคอลัมอยู่ระดับ+ขนาดเดียวกัน**
- 🔧 คอลัม **ชื่อลูกค้า · ที่อยู่ · รายการสินค้า · โน๊ตล่าสุด · หมายเหตุ** ตั้ง `vertical-align:top` ไว้ ทำให้ข้อความลอยสูงกว่าคอลัมอื่น (ที่เป็น middle) — เอาออก ใช้ middle เหมือนกันหมด · ตอน**กางแถว**ยังชิดบนพร้อมกันเหมือนเดิม (`tr.rowopen`)
- 🔧 **โน๊ตล่าสุด/หมายเหตุ** ขนาดตัวอักษรเล็กกว่าคอลัมอื่น (.78rem vs .84rem) — ปรับให้เท่ากันทุกคอลัม
- _วัดจริง: ทุกคอลัม 13.44px · กลางแนวตั้งต่างกัน <1px_

**2) modal นำเข้า: เลิกใช้แท็บ → หน้าเดียวแบ่ง 2 ส่วน**
- 🔄 **ซ้าย = อัพไฟล์ · ขวา = ประวัตินำเข้า** (เห็นพร้อมกัน ไม่ต้องสลับโหมด) — ทั้ง modal ออเดอร์ GoSell และ COD · modal กว้างขึ้นเป็น 58rem เฉพาะหน้านี้ · หน้าตรวจไฟล์/preview ใช้ขนาดเดิม (34rem)
- 🔄 ตารางประวัติ: **เอาเส้นแบ่งคอลัมตั้งออก** (เส้นมีแค่ในหัวตาราง ขาดห้วนตรงรอยต่อ) → ใช้**แรเงาอ่อน**ไล่ต่อเนื่องทั้งหัวและตัวตารางบอกกลุ่ม "ตั้งแต่–ถึง" แทน · ปรับ padding/ขนาดให้พอดีช่องไม่ล้น
- 🔧 modal ไม่ย่อกลับตอนขึ้น error ไฟล์ผิดนามสกุล — แก้แล้ว
- 🔧 คลาส `.r` ที่ใช้ชั่วคราวชนกับคลาสสีป้ายแดงของระบบ (พื้นหลังแดงทั้งแผง) — เปลี่ยนเป็น `.right`

### `6a1f2fd` · 10:22 — แก้ไอคอนช่องค้นหา (หน้าออเดอร์ตีกลับ)
- 🔧 **แว่นขยายแสดงเป็นจุดเทาทึบ** — ใส่ `fill` + `stroke:none` ทับไอคอนแบบเส้น (`i-search` = วงกลม+ด้ามจับ) ทำให้วงกลมถูกถมและด้ามจับหาย · แก้เป็น `color` ตาม default ของระบบ (`stroke:currentColor; fill:none`) · โฟกัสช่องแล้วไอคอนเปลี่ยนเป็นสีน้ำเงินด้วย
- 🔧 ปุ่ม **× ล้างคำค้น** ผิดแบบเดียวกัน (`i-close` ก็เป็นไอคอนเส้น) — แก้แล้ว

### `e249c6d` · 10:19 — ค้นหาในหน้าออเดอร์ตีกลับ + คีย์ลัด Cmd/Ctrl+F
- ➕ **ช่องค้นหาในหน้าออเดอร์ตีกลับ** (ข้างหัวตาราง) — ค้น **เบอร์โทร · ชื่อลูกค้า · ที่อยู่ · เลขแทร็คส่งออก** แบบเดียวกับหน้าออเดอร์ · ไม่สนตัวพิมพ์เล็ก/ใหญ่ · debounce 160ms · ปุ่ม × ล้างคำค้น · Esc ล้าง · ทำงานร่วมกับตัวกรองคอลัม (cross-filter นับตามคำค้นด้วย)
- ➕ **คีย์ลัด `Cmd+F` / `Ctrl+F`** → สลับไปหน้า **ค้นหา** + วางเคอร์เซอร์ในช่องค้นหาให้พร้อมพิมพ์ (แทน find ของเบราว์เซอร์) · ถ้าอยู่หน้าค้นหาแล้วกดซ้ำ = โฟกัสช่องเดิม (ไม่โหลดหน้าใหม่) · เช็คสิทธิ์ role ก่อนสลับหน้า

### `1885d6e` · 10:12 — feedback ผู้ใช้: ยุบสถานะเป็น "กำลังส่ง" + ประวัตินำเข้า + ปรับคอลัมตาราง
**1) ยุบสถานะจัดส่ง "รอส่ง" + "ส่งแล้ว" → "กำลังส่ง"** (🟡 เหลือง · ไอคอนรถบรรทุก)
- 🔄 สถานะจัดส่งเหลือ **5 ค่า**: กำลังส่ง · ส่งสำเร็จ · ตีกลับ · ยกเลิก · มีปัญหา (แก้หลายรายการเหลือ 3: กำลังส่ง/ส่งสำเร็จ/ยกเลิก)
- 🔄 **ย้ายข้อมูล 49 ออเดอร์** (รอส่ง 45 + ส่งแล้ว 4) → กำลังส่ง · backup `_bak_deliv_status_20260914`
- 🔄 backend: CHECK constraint · DEFAULT คอลัม · `app_save_order_tracking` · `app_bulk_set_delivery` · `app_import_orders` · `get_months` (เกณฑ์เดือน ✓)
- 🔄 นำเข้า GoSell: **กำลังแพ็ค · พร้อมจัดส่ง · จัดส่งแล้ว** → กำลังส่ง (รวม 3 ค่า) · ค่าไม่รู้จักก็ลงกำลังส่ง
- 🔧 หน้าค้นหา: ตัวหาร "ส่งออกแล้ว" ของ %สำเร็จ/%ตีกลับ ใช้สถานะใหม่ (ไม่งั้นเปอร์เซ็นต์เพี้ยน)
- ℹ️ **ประวัติเก่าไม่ถูกแตะ** — คงตัวแปลงสีของ 2 สถานะเดิมไว้ ป้ายใน timeline ยังแสดงถูก (1,162 รายการ)

**2) ตาราง: คอลัมโน๊ตล่าสุด/ติดตามล่าสุด**
- 🔄 ขยาย **โน๊ตล่าสุด** 137 → **181px** (เท่าคอลัมเลขแทร็ค)
- 🔄 สีตัวอักษร **ติดตามล่าสุด + โน๊ตล่าสุด** เทา → **ดำ** ("วันนี้" ยังน้ำเงินตัวหนาเหมือนเดิม · คอลัม "หมายเหตุ" ยังเทา)
- ➕ **ตัวกรอง "ติดตามล่าสุด"** โชว์คำว่า **"วันนี้"** แทนค่าวันที่ (ตรงกับในตาราง) · ดันขึ้นบนสุดของรายการ

**3) ประวัติการนำเข้า (ใหม่)**
- ➕ modal นำเข้า **ออเดอร์ GoSell**: แท็บ `อัพไฟล์ | ประวัตินำเข้า` — ตาราง **วันที่ทำรายการ · ช่วงข้อมูล (ตั้งแต่–ถึง) · จำนวนออเดอร์ · ผู้ทำรายการ** (header 2 ชั้น · ใหม่สุดอยู่บน)
- ➕ modal นำเข้า **COD**: แท็บเดียวกัน — **วันที่ทำรายการ · ประเภท (ไฟล์/แก้มือ) · จำนวนรายการ · ผู้ทำรายการ**
  *(COD ไม่มีคอลัมช่วงวันที่ เพราะไฟล์ต้นทางไม่มีข้อมูลวันที่)*
- ➕ backend RPC `app_import_history(p_kind)` — อ่านจาก `audit_log` ที่บันทึกไว้แล้ว (ไม่ต้องสร้างตารางใหม่ · มีประวัติย้อนหลังทันที) · สิทธิ์ Adm/OM
- ℹ️ แท็บโชว์เฉพาะหน้าเลือกไฟล์ · หลังอัปโหลดแล้วซ่อน (กันสถานะตรวจไฟล์หาย)

## 2026-09-12

### `39c1e7b` · 15:34 — ตาราง: คอลัม ติดตามล่าสุด/โน๊ตล่าสุด อัปเดตสดทันที
- 🔧 **เพิ่มโน้ต/เปลี่ยนสถานะผ่าน sidebar** → คอลัม "ติดตามล่าสุด" + "โน๊ตล่าสุด" ในตารางอัปเดตทันที (เดิมต้องรีเฟรช/โหลดเดือนใหม่ก่อน) · ดึงจาก `timeline` ที่ RPC คืนมาอยู่แล้ว (สูตรตรง `get_orders`) — ไม่ต้องแตะ backend
- 🔧 **แก้โน้ตใน timeline (sidebar)** → คอลัม "โน๊ตล่าสุด" อัปเดตเฉพาะเซลล์นั้นทันที (เงียบ ไม่กระพริบ)
- ➕ โบนัส: เปลี่ยนสถานะ inline ดัน "ติดตามล่าสุด" เป็นวันนี้สดด้วย

## 2026-09-11

### `1bba964` · 13:58 — รายการตีกลับ: dropdown ทีม/พนักงานแบบใหม่ + จุดเขียวมาร์คคนมีตีกลับ
- 🔄 **dropdown ทีม + พนักงาน** (หน้ารายการตีกลับ) เปลี่ยนจาก native select → custom: **เปิดใต้ปุ่มลงล่าง** · ตกแต่งใหม่ (ขอบมน เงา hover · ติ๊กน้ำเงินหน้าตัวที่เลือก) · ปิดเองเมื่อเลือก/คลิกนอก
- ➕ **จุดเขียวชิดขวา** ในรายการพนักงาน = คนที่มีตีกลับในรอบเดือน+โหมดนั้น (ไม่มี = เว้นว่าง) · รีเฟรชอัตโนมัติตามรอบ/โหมด
- ➕ backend `app_returns_list` เพิ่มฟิลด์ `sellers_with_returns` (รายชื่อ employee_code ที่มีตีกลับในรอบ/ทีมที่เลือก · ไม่ขึ้นกับ filter พนักงาน)

### `cbd8efe` · 13:32 — รีเซ็ตรหัสผ่าน: เลิกบังคับความซับซ้อน
- 🔄 **หน้าตั้งรหัสใหม่ (จัดการผู้ใช้)**: เอาการบังคับความซับซ้อนออก (เดิมบล็อกถ้าไม่ผสมพิมพ์ใหญ่/เล็ก+ตัวเลข+สัญลักษณ์) · ลดขั้นต่ำ 8→**6 ตัว** ให้ตรงกับ backend · ตั้งรหัสเลขล้วนได้แล้ว (มี OTP กันไว้อีกชั้น) · ยังกันช่องว่างไว้กันรหัสมองไม่เห็น · แถบวัดความแข็งแรงยังโชว์เป็นข้อมูล (ไม่บล็อก)

### `f412fa4` · 13:13 — EDITH: เคสไม่หักยอด + แก้กระพริบ + audit ปฏิทิน + KPI ตีกลับ
- ➕ **EDITH เคส recon**: เพิ่ม **เบอร์โทร + วันที่สั่งซื้อ** · โชว์ "ลงตีกลับแบบ หักยอด/ไม่หักยอด"
- ➕ ปุ่ม **"ปรับเป็นถึงแล้ว"** (RPC `app_edith_exchange`) — ส่งสำเร็จ+ชำระแล้ว+ถึงแล้ว · เก็บทั้ง COD/ตีกลับ ไม่ลบ · **ไม่แตะ no_deduct** (ตั้งที่หน้าบันทึกตีกลับเท่านั้น)
- 🔧 **แก้ EDITH กระพริบ** — poll 45 วิ เป็นแบบ quiet (ไม่โชว์ spinner + ไม่วาดใหม่ถ้าข้อมูลเดิม)
- ➕ **Audit log: ปฏิทินลากเลือกช่วง** (ยุบเป็นช่องสรุปได้) + **custom time picker** (HH|MM 00→สุด ไม่ลูป) แทน datetime-local เดิม · + user filter
- 🔄 **KPI ยอดตีกลับถึงแล้ว** = `return_arrived && delivery=ตีกลับ` (เคสส่งสำเร็จ+ถึงแล้ว หลุดจากยอดตีกลับ · ค่าเดิมไม่เปลี่ยน)
- _(backend RPC `app_edith_exchange`/`app_edith_recon_detail`+ordered_date อยู่ DB แล้ว)_

### `de71af4` · 09:43 — แก้ scrollbar แนวนอนเดิมโผล่ตอนเลื่อน
- 🔧 ปิด scrollbar แนวนอนเดิมของตารางทุกแบบ (รวม overlay macOS ที่โผล่ตอนเลื่อนแล้วดูซ้อนแปลก) ด้วย `overflow-x:hidden` — 3 หน้า (ออเดอร์/ตีกลับ/ค้นหา) · เหลือแถบเลื่อนใต้ตารางตัวเดียว
- ➕ wheel handler: trackpad ปัดแนวนอน + shift+wheel ยังเลื่อนตารางซ้ายขวาได้ (เลื่อนแนวตั้งปกติไม่กระทบ)

### `b5eed37` · 09:22 — infra: ระบบ CHANGELOG + ข้ามการ deploy เมื่อแก้เอกสาร
- ➕ เพิ่ม `CHANGELOG.md` (โปรเจค MO) + `CLAUDE.md` (ราก Face Advance Systems · กฎ: ทุก deploy ต้องบันทึก changelog · ใช้ทุกโปรเจค)
- 🔄 GitHub Actions: แก้ไฟล์ `.md` อย่างเดียวจะไม่ trigger deploy (`paths-ignore`)
- _(ไม่มีการเปลี่ยนแปลงตัวแอป)_

---

## 2026-09-10 (13 deploy · สำเร็จทุกตัว)

### `d294eb1` · 17:10 — แก้สถานะหลายรายการ + cross-filter + เลือกแทร็คแบบชุด/ลาก
- ➕ **แก้สถานะจัดส่งหลายรายการทีเดียว**: ลากที่ดินสอ→วงกลมเลือกอัตโนมัติ (ไม่จำกัดจำนวน) · หัวคอลัมโชว์ดินสอ+จำนวน+× · dropdown 4 สถานะ (รอส่ง/ส่งแล้ว/ส่งสำเร็จ/ยกเลิก · ไม่มีตีกลับ/มีปัญหา) · ต้องยืนยันก่อนบันทึก · เลือก "ยกเลิก" → ตั้ง payment=ยกเลิก ด้วย · backend RPC set-based `app_bulk_set_delivery` + log ทุกออเดอร์
- ➕ **Cross-filter** (เหมือน Google Sheet): ตัวกรองแต่ละคอลัมนับเฉพาะแถวที่ผ่านตัวกรองคอลัมอื่นแล้ว — 3 หน้า (ออเดอร์/ตีกลับ/ค้นหา)
- ➕ **เลือกเลขแทร็คแบบชุดละ 30** (ปุ่ม ↑↓ ซ้อนแนวตั้ง) + **ลากเลือกหลายแถว** + auto-scroll + กัน text selection
- 🔄 ย้าย **scrollbar แนวนอนไปไว้ใต้ตาราง** (แก้ Windows scrollbar เดิมจับยาก · ซ่อนตัวเดิม · wheel/trackpad ยังเลื่อนได้) — 3 หน้า
- 🔧 แก้บั๊ก **สามเหลี่ยมกาง/ยุบหาย** ตอนเปิดแถวดูรายละเอียด
- 🔄 ขยายคอลัม "ปัญหา" + "โน๊ตล่าสุด" +20% (114→137px)

### `0d55e27` · 14:56 — ถอดสถานะเซ็นรับ + โน๊ตล่าสุด + คัดลอกแทร็ค
- ➖ **เอาสถานะ "เซ็นรับ" ออกทั้งหมด** (ทดลองช่วง 13:51–14:05 แล้วเจ้านายไม่เอา)
- ➕ คอลัม **"โน๊ตล่าสุด"** (ข้อความโน้ตล่าสุด)
- ➕ ปุ่ม **แก้ไขโน้ต** เฉพาะโน้ตวันนี้ที่ตัวเองพิมพ์
- ➕ ปุ่ม **คัดลอกเลขแทร็ค** (เอาการกดเปิดลิงก์ออก)

### `7072a82` · 14:23 — คอลัมลากจัดลำดับ
- ➕ เมนูคอลัม **ลากจัดลำดับได้** (drag reorder) + จำต่อเครื่อง

### `82f7939` · 14:13 — คอลัม "ปัญหา"
- 🔄 "รายละเอียดปัญหา" → **"ปัญหา"**: โชว์ข้อความตรงๆ ตัด ... + สามเหลี่ยมกาง · กว้าง 114 · เลิก popup ไอคอน

### `15cbd32` · 14:05 · `84dfa57` · 13:58 · `5aafc2c` · 13:54 · `4c819e1` · 13:51 — (ทดลองสถานะเซ็นรับ · ถอดออกภายหลัง)
- ➕🔄🔧 เพิ่มสถานะ "เซ็นรับแล้ว" → เปลี่ยนสีม่วง → แก้บั๊ก badge สีชนหัวการ์ด (`.badge.t`) → เปลี่ยนชื่อเป็น "ไม่เปลี่ยนเป็นเซ็นรับ"
- ⚠️ ทั้งชุดนี้ถูก ➖ เอาออกใน `0d55e27` (สรุปสุดท้ายไม่มีในระบบ)

### `6a5b897` · 13:12 · `6a4f847` · 13:06 — คอลัม "ติดตามล่าสุด"
- ➕ คอลัม **"ติดตามล่าสุด"** (ก่อนหมายเหตุ) = วันที่ user อัพเดตล่าสุด · วันนี้แสดง "วันนี้" (น้ำเงิน) · อื่นๆ วันที่สีเทา

### `9df413e` · 11:55 — Sidebar + จัดการผู้ใช้
- 🔄 Sidebar: เบอร์โทรเอาขีดออกทั้งหมด
- ➕ เตือนเมื่อปิดทั้งที่ยังไม่เซฟ + **กู้ร่างอัตโนมัติ**ตอนเครื่องดับ
- 🔄 จัดการผู้ใช้: idle timeout สูงสุด 480 → **1440 นาที**
- 🔧 บังคับเลือกทีมเฉพาะ role ที่ยึดทีม (RT+/RTs) · Adm/OM/Vm ไม่ต้อง

### `24dc35e` · 09:24 · `a7fff7c` · 09:18 — แก้บั๊ก COD แก้มือ
- 🔧 Sidebar รับเงิน COD แก้มือ ไม่เปิด (ลืม append overlay เข้า DOM)
- 🔧 Sidebar COD แก้มือ ค้างนอกจอ (ลืมใส่ class `.show`)
