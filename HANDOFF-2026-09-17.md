# HANDOFF — ระบบ MO · 2026-09-16 ถึง 17

> สรุปส่งต่อ เก็บเฉพาะ **การตัดสินใจ · สถานะสุดท้าย · กฎ · งานค้าง** (ตัดทางตัน/ลองผิดลองถูก/log ออก)

## 🗄️ สถานะข้อมูลในระบบ (prod `xfayguljywhjwqcuimvw`)

| | |
|---|---|
| ออเดอร์จริง | **64,648 ใบ · 108,329,553 บาท** · ม.ค.–ก.ย. 2569 |
| ข้อมูลจำลอง | **10,000 ใบ ธ.ค. 2025** `order_no LIKE 'MOCK2512-%'` ⚠️ **ต้องลบเมื่อเทสเสร็จ** |
| COD | 57,325 แถว (ย้อนหลัง ม.ค.–มิ.ย. 43,238 แถว · 72,549,178 บาท) |
| บันทึกตีกลับ | 130 แถว (ก.ย. เท่านั้น) — **ย้อนหลังยังไม่นำเข้า** |
| คิว dedup | 0 (พนักงานเคลียร์แล้ว · รวมลูกค้าไป 246 ครั้งวันที่ 16) |

## ✅ สิ่งที่ทำเสร็จ

**2026-09-16** — นำเข้าออเดอร์ย้อนหลัง ม.ค.–มิ.ย. **48,608 ใบ · 80,610,554 บาท** จาก `database/2026.xlsx`
· `order_no` = `HIST26MM-NNNNN` · ลูกค้าใหม่ 29,362 · ตัวแทน 136 ใบ · ที่อยู่แยก 5 ฟิลด์สำเร็จ 100%
· ตรวจย้อนกับไฟล์ต้นฉบับทีละแถวทุกฟิลด์ = **0 ต่าง**
· deploy `4648c41` — ปุ่ม "รวมทันที" · ป้าย "≈ คล้ายกัน" · แก้บั๊กค่าแสดงติดกัน

**2026-09-17** — deploy `6d4622b`
1. `delivery=ยกเลิก` → ผูก `payment=ยกเลิก` (backend + ชิปหน้าเว็บ) · แก้ข้อมูลย้อนหลัง 26 ใบ
2. COD `received_from='ทำเคลม'` → ไม่ตั้ง `delivery=ส่งสำเร็จ`
3. ตารางไม่เด้งขึ้นแถวแรก: ล้างคำค้น→ตำแหน่งเดิม · ตัวกรอง/เรียง→ยึดแถวบนสุด
4. นำเข้า COD ย้อนหลัง 43,238 แถว (แยกแถว `-B` ให้ตรงออเดอร์ปนแบรนด์ 8 คู่)
5. สร้างข้อมูลจำลอง ธ.ค. 2025

## 🔴 งานค้าง (ลำดับความสำคัญ)

### 1. นำเข้าตีกลับย้อนหลัง ม.ค.–ส.ค. — **รอเจ้านายเคาะ**
ไฟล์ `database/รายการตีกลับ.xlsx` (1,785 แถว) · รายงาน `database/รายการตีกลับ-ตรวจแล้ว.xlsx`
staging อยู่ใน `public._stg_ret` แล้ว (ผลตรวจว่าง 3 แถว → ตั้ง `สินค้าครบ ไม่เสียหาย` ไปแล้ว)

| ชีต | แถว | ต้องเคาะ |
|---|---|---|
| A. ผลตรวจระบบไม่รับ | 4 | `สินค้าสูญหาย`→`สินค้าไม่ครบ` · `บรรจุภัณฑ์เสียหาย`→`สินค้าเสียหาย` (เสนอไว้แล้ว) |
| B. ไม่มีรายละเอียดเสียหาย | 8 | CHECK บังคับ · เสนอใส่ `"ไม่มีบันทึกรายละเอียด (นำเข้าย้อนหลัง)"` |
| C. เลขแทร็คซ้ำ | 2 | `7027053840510866` ซ้ำ 2 แถว (UNIQUE) เลือกเก็บแถวเดียว |
| E. ออเดอร์ไม่ใช่ตีกลับ | 41 | ส่งสำเร็จ/ชำระแล้ว (ส่วนใหญ่มีเงิน COD) → นำเข้าไหม? อาจเป็นเคสแลกเปลี่ยน |
| F. ข้อมูลไม่ครบ | 3 | ไม่มีแทร็คตีกลับ+รูป (DB ยอมว่าง) |
| ขาดในไฟล์ | 6 | ตีกลับในระบบที่ไม่มีในไฟล์ (ส.ค. 5 + ก.พ. 1) |
| ✅ พร้อมนำเข้า | **1,727** | |

**ผลที่จะเกิด**: `payment` → `ยกเลิก` · `return_arrived=true` · timeline ~2 แถว/ใบ · ใบที่มี COD → `recon_conflict`

### 2. ลบข้อมูลจำลอง ธ.ค. 2025 เมื่อเทสเสร็จ
```sql
delete from public.orders where order_no like 'MOCK2512-%';
delete from public.customers where id in (select id from public._bak_mock_dec2025 where kind='customer');
```

### 3. ค้างอื่นๆ
- `app_returns_list` โหมดหักยอดใช้ **INNER JOIN `sellers`** (บรรทัด 127,138) → ออเดอร์ `seller_id` ว่างถ้าตีกลับจะหายเงียบ · ตอนนี้ยังไม่กระทบ → ควรแก้เป็น `left join`
- 8 คู่ออเดอร์แยก `-B` กับ COD: ถ้านำเข้าไฟล์ COD ที่มีแทร็คเหล่านี้อีกจะยอดไม่ตรง
- ค่าคอม "ไม่หักยอด" สูตร · go-live: reset 5 RTs passwords · fix `jj87` ในไฟล์พนักงาน
- responsive มือถือ/iPad ⏸️ (ยังไม่กำหนด stage)
- ลบ backup เมื่อตรวจผ่าน: `_bak_hist_import` · `_bak_cod_hist_import` · `_stg_*` · `_bak_testret_*` · `_bak_cancel_pay_20260917` · `_bak_manual_fix_20260917` · `_bak_agent_paystatus_20260916` · `_bak_merge_undo_20260916`

## 📏 กฎ/ข้อจำกัดที่ต้องจำ

**นำเข้าข้อมูลใหญ่ → ทำฝั่ง DB ตรงด้วย `psql` (role `postgres`) ไม่ใช่เว็บแอป**
`SUPABASE_DB_URL_FACEADVANCE` ใน `~/.claude/secrets.env` · `app_import_orders` วน insert ทีละแถว (0.932 ms/ใบ) + `detect_customer_duplicates` (17.5 นาที) เกิน `anon` timeout 20s หลายสิบเท่า · **ไม่ต้องแตะ anon timeout** · สคริปต์ตัวอย่าง `database/import-scripts-2026/`

**ลำดับ deploy** (พลาดมาแล้ว 2 ครั้ง)
- frontend อ่านค่าใหม่ → **deploy frontend ก่อน** DB ทีหลัง
- frontend เป็นคนตัดสิน **ข้อความ/การตีความ** → **ห้ามเปลี่ยนความหมายของค่าจาก backend เดี่ยวๆ** (เคส `hit` → ป้าย "ตรงกัน" กลายเป็นโกหก)
- เพิ่ม **ฟิลด์ใหม่** ที่ของเก่าข้ามได้ → ลำดับไหนก็ได้

**ต้องเทสเองก่อนรายงานทุกครั้ง** — เคย push งาน scroll โดยไม่เทส มีบั๊ก `preSearch = null` วางนอก `if` ที่เทสครั้งเดียวก็เจอ

**อื่นๆ**
- ตรวจ bundle บน prod: Thai string ถูก escape → ต้องถอด `\uXXXX` ด้วย Python ก่อน grep · ชื่อฟังก์ชันภายในถูก minify ทิ้ง ให้ดูชื่อ property แทน
- `recon_returns` มี **UNIQUE บน `btrim(tracking_out)`** (1 ออเดอร์ = 1 บันทึก) · `recon_cod_payments` **ไม่มี** unique
- `inspection_result` รับ 4 ค่า · ถ้า ≠ `สินค้าครบ ไม่เสียหาย` **ต้องมี `damage_detail`** (CHECK)
- รอบค่าคอม = **ตัดวันที่ 26** คิดจาก `recon_returns.recorded_at` (รอบ มี.ค. = 26/02–25/03)
- `orders.phone` CHECK `^[0-9]{8,15}$` NOT NULL · `payment_method` รับ เก็บเงินปลายทาง/โอนเงิน/ตัดบัตรเครดิต
- ตัวแทน: ออเดอร์ที่มีสินค้า `เอกสาร` → brand ถูกบังคับเป็น `ตัวแทน` (ปนแบรนด์ได้) · `payment=ไม่ใช่งานขาย` · `seller_id` ว่าง
- รายงานยอดขาย **ต้องกรอง `MOCK2512-` ออกเสมอ**
- ฟรายเดย์ **mint session token ไม่ได้** (guard บล็อก) → เทส UI ต้องใช้แท็บที่เจ้านาย login ไว้ หรือเรนเดอร์ preview เอง
